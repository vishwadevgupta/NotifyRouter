# Set to true only after adding an API key. False gives deterministic on-device/server routing.
LLM_ENABLED=false
OPENAI_API_KEY=
ROUTER_MODEL=gpt-4.1-mini
# An inbound API key protects this service when it is publicly exposed.
ROUTER_API_KEY=change-me-before-deploying
